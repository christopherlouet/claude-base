# Module "mobile" — framework-AGNOSTIC mobile app lifecycle: store release +
# mobile testing. The Flutter framework itself is a separate, mutually-exclusive
# choice — see the "flutter" module (a React Native project would opt into
# `mobile` + a future `react-native` module instead). Spans ops/qa. Same syntax
# as minimal-manifest.txt: one repo-relative path per line, # comments and empty
# lines ignored, trailing / = directory.
# Drift guard: tests/modules.bats asserts every path exists and no overlap.

# === Commands (2) ===
.claude/commands/ops/ops-mobile-release.md
.claude/commands/qa/qa-mobile.md

# === Skills (1 directory) ===
.claude/skills/ops-mobile-release/
