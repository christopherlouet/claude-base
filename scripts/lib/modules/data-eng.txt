# Module "data-eng" — data engineering: pipelines (ETL/ELT) + warehouse modeling.
# Owns the whole data/ domain (it leaves the universal core entirely).
# Same syntax as minimal-manifest.txt: one repo-relative path per line,
# # comments and empty lines ignored, trailing / = directory.
# Drift guard: tests/modules.bats asserts every path exists and no overlap.

# === Commands (2) ===
.claude/commands/data/data-pipeline.md
.claude/commands/data/data-modeling.md

# === Agents (1 — data-modeling collapsed, passthrough; Wave 1) ===
.claude/agents/data-pipeline.md

# === Skills (1 directory) ===
.claude/skills/data-pipeline/
